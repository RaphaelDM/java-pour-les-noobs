services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: booksdb
      POSTGRES_USER: books
      POSTGRES_PASSWORD: books
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
